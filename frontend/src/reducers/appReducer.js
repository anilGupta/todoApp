import { types } from '../constants/ActionType';

const InitialState = {

};

export default function(state = InitialState, action) {
  switch (action.type){

      default:
          return state;
  }
}
